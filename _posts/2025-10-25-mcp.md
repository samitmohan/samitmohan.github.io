---
layout: post
title:  "mcp servers"
date:   2025-10-25 14:06:04 +0530
categories: tech
tokens: "~1.1k"
description: "How the Model Context Protocol works and why it matters for AI tool integration."
---

MCP (Model Context Protocol) servers are like web APIs, but built for LLM interactions. With them, language models like Claude can do things, not just talk about them. MCP is an open protocol created by Anthropic, and Claude Desktop has native support for it.

Before MCP, every AI application had to build its own custom integration for each external tool or data source - N models times M tools meant N*M separate connectors. MCP solves this by providing a single standardized protocol for connecting AI models to external capabilities, similar to how USB standardized peripheral connections so that any device works with any computer through one interface.

I’m always listening to music and creating playlists on Spotify, and I use LLMs daily in my browser. But until now, there was no easy way to control Spotify using an LLM.

That’s where MCP Servers come in.

#### Why LLMs Don’t Recognize Spotify Natively

![LLMs Doesn't Recognise Spotify](/assets/images/mcp/llm-no-spotify.webp){: loading="lazy"}


You can read more about them [here](https://modelcontextprotocol.io/examples)

#### How It Works

![How it Works](/assets/images/mcp/how-it-works.webp){: loading="lazy"}

#### What I Wanted to Do

- Play, pause, and skip tracks via Claude
- Queue songs by name or mood
- Get quick info about the music
- Make playlist according to 'vibes' 
- Ability to manipulate music and my current playlists on Spotify

### Model

Under the hood, I used the Spotipy Web API plus the MCP integration for Claude. After setting up Spotify credentials and installing the right dependencies, here is a sample of server.py file:

```python
# server.py
import mcp.server.fastmcp
from spotipy import Spotify
from spotipy.oauth2 import SpotifyOAuth

auth = SpotifyOAuth(
    client_id="id",
    client_secret="secret",
    redirect_uri="uri",
    scope="user-modify-playback-state user-read-playback-state user-read-currently-playing",
    cache_path=".cache",
    open_browser=True
)

# caches the access token to avoid repeatedly requesting permission from Spotify
token_info = auth.get_access_token(as_dict=False)
sp = Spotify(auth_manager=auth)

# Create an MCP server
server = mcp.server.fastmcp.FastMCP("Demo")

# Spotify tools
@server.tool()
async def play_song(song_name: str) -> str:
    results = sp.search(q=song_name, limit=1, type='track')
    if results['tracks']['items']:
        track = results['tracks']['items'][0]
        sp.start_playback(uris=[track['uri']])
        return f"Playing {track['name']} by {track['artists'][0]['name']}"
    return "Song not found"

@server.tool()
async def pause() -> str:
    sp.pause_playback()
    return "Paused"

if __name__ == "__main__":
    server.run(transport='stdio')
```

You also need to integrate this into Claude (Desktop Local App) via the claude_desktop_config.json file:

```json
{
  "mcpServers": {
      "spotify": {
          "command": "~/.local/bin/uv",
          "args": [
              "--directory",
              "~/fun/mcp-server/mcp-server-demo",
              "run",
              "server.py"
          ]
      }
  }
}
```

Then run the server:

```bash
uv run server.py
```

**Now Claude can use your Spotify tools!**

![MCP Tools in Claude](/assets/images/mcp/mcp-tools-claude.webp){: loading="lazy"}

### In action

![Claude Talking to Spotify](/assets/images/mcp/claude-spotify-action.webp){: loading="lazy"}

![Spotify2](/assets/images/mcp/spotify-2.webp){: loading="lazy"}

![Spotify3](/assets/images/mcp/spotify-3.webp){: loading="lazy"}

[Video Link](https://youtu.be/BExl8jhthoE)

### What it can do

The highlights: play/pause/skip, queue songs, create playlists by mood or vibe, get info on what’s playing, shuffle/repeat, pull up lyrics (via Genius API), and browse your liked songs and recently played. Basically full Spotify control through natural language.

---

Spotify unfortunately is lame. No use of localhost or support for custom HTTPS + removes all fun features like analysing audio. They did this last week, guess I’m late to the AI learning hype. But MCP servers are growing quickly - see other examples at the [official repo](https://github.com/modelcontextprotocol/servers) 

Had to use ngrok for tunneling, which seems excessive for a localhost server. Other than that the python stuff is pretty basic and mcp does the magic.

I am surprised by how fast I was able to build this, probably because of the spotipy api but even without that, it won’t take that much time.

You don't need the Spotipy wrapper - you could also hit the Spotify REST API directly from your MCP tool:

```python
# Raw API sample (if not using Spotipy)
import requests

def play_song(access_token, track_uri):
    url = "https://api.spotify.com/v1/me/player/play"
    headers = {"Authorization": f"Bearer {access_token}", "Content-Type": "application/json"}
    data = {"uris": [track_uri]}
    return requests.put(url, headers=headers, json=data).status_code
```

LLMs have been mostly observers, not doers.

Would be pretty cool if everyone can control their 3rd party apps via LLMs. Creating a trading account bot which talks to LLM or flight booking APIs so I don't have to open MMT or Zerodha everytime I've to purchase a ticket/stock should be easy now. I can just tell Claude to do it for me.

[Github](https://github.com/samitmohan/spotify-mcp)